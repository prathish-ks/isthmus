# Customization Regression Catalogue (P6-01)

Status: written 2026-09-02, as task P6-01. Prerequisite: P5-06 (satisfied — Phase 5 closed, commit `63d6b648` + macOS symlink hotfix). Source for the 20-scenario requirement and category list: `claude/nanoclaw-go-host-master-plan.md`'s P6-01 definition. Source for layer classification: `docs/host-decomposition.md` (P1-01) and `docs/design-laws.md` (LAW-01/LAW-02, and the LAW-07 annotation for the handful of scenarios that legitimately touch the Go boundary as data, never as code).

## Purpose

LAW-02 says a normal user must be able to ask Claude/Codex to change workflows, prompts, channels, or integrations without touching the Go kernel. That's a design intention until it has executable acceptance criteria. This catalogue is those criteria: 20 concrete NanoClaw customizations, each classified by which layer changes and whether Go source is expected to change. The default, per the task's own instruction, is **No** — Go should not change for ordinary customization. P6-03 proves ≥10 of these with zero Go edits; P6-05 computes the resulting preservation percentage across all 20.

A handful of scenarios below are marked "No — but exercises the Go boundary as data." These are deliberately included, not omitted for tidiness: they are the customizations most likely to *look* like they need a Go change (because they interact with mount classes, session ownership, or resource limits — Phase 5's own territory) but don't, because the boundary was designed exactly to let flexible data flow through fixed Go validation. Getting this distinction catalogued explicitly is what lets P6-05 tell a real architectural defect (an unexpected Go edit) apart from a customization that was never going to be Go-free in the naive sense but is still LAW-02-compliant.

## The 20 scenarios

| # | Scenario | Category | Layer touched | Go change? | Rationale |
|---|---|---|---|---|---|
| 1 | Change a wired agent's engage trigger word/pattern | trigger word | Data: `messaging_group_agents.pattern` column (config, no code) | **No** | `routing.EvaluateEngage` (Go, P4-02) already takes `pattern` as a runtime parameter. Changing the string is a data edit reaching an existing, unmodified Go function through P6-02's `route.request` op. |
| 2 | Rename an agent group | agent name | Data: `agent_groups.name` (central DB) | **No** | Never read by any Go package — `mount.Session`/`session.Session` key on `AgentGroupID`, not display name. Pure TS/DB concern. |
| 3 | Customize the container's welcome/greeting message | greeting | Container skill (`container/skills/welcome/`) | **No** | host-decomposition.md #15: agent-runner and its container skills are KEEP TYPESCRIPT/Bun by design; nothing here reaches `src/` let alone `go-host/`. |
| 4 | Add Slack-specific message formatting (mrkdwn) | formatting | Container skill (`slack-formatting`, channels branch) | **No** | Formatting skills are agent-container-side per CONTRIBUTING.md's skill-type #4; no host-side code, Go or TS, is touched. |
| 5 | Add a custom access-gate rule (e.g. VIP sender bypass) | custom route | `src/modules/permissions/index.ts` (access-gate hook) | **No — but exercises the Go boundary as data** | host-decomposition.md #2/#11: the access gate is a router.ts hook seam, explicitly KEEP TYPESCRIPT. Its boolean *result* crosses into `route.request`'s `accessAllowed` field (P6-02) — `routing.DecideWiringOutcome` (Go) still runs unmodified against whatever the new TS rule decided. |
| 6 | Add a new utility skill with bundled code | skill | `.claude/skills/<name>/` (skill directory) | **No** | CONTRIBUTING.md skill-type #2: self-contained, copied into place at install; no host process code at all. |
| 7 | Add a new MCP tool via a container skill (OneCLI-gateway pattern) | MCP tool | `container/skills/<name>/` | **No** | Same boundary as #3/#4 — container-side, mounted read-only, never a host or kernel code path. |
| 8 | Change a scheduled task's recurrence rule | scheduler | `scheduling/recurrence` module (dynamic-import hook from `host-sweep.ts`) | **No** | host-decomposition.md #6: `decideStuckAction` (the only Go piece of host-sweep) is pure liveness arithmetic and has no recurrence concept; the recurrence module is an orchestration-layer hook that stays TS. |
| 9 | Run two agents in the same group against the same channel | multiple agents | `agent_groups`/wiring config (data) + `container-runner.ts` composition | **No — but exercises the Go boundary as data** | Each agent gets its own `SessionKey.AgentGroupID`; `mount.ValidateSpec`'s group-scope/`GROUP_FOLDER_LABEL` pinning (Go, Phase 5) must accept two distinct, correctly-scoped specs without modification — proving the isolation invariant holds for N agents, not just one, without touching `internal/mount`. |
| 10 | Add a new messaging channel (Discord) | channel | `channels` registry branch + `.claude/skills/add-discord/` | **No** | CONTRIBUTING.md skill-type #1: install is a `git show`-based fetch-and-copy from a long-lived branch, never a Go or core-TS edit. |
| 11 | Customize the composed system prompt / CLAUDE.md | prompts | `project-doc-compose.ts` | **No** | Pure TS composition; nothing in `go-host/` reads or shapes prompt content — confirmed by `mount-composition.test.ts`'s own mock of `composeGroupProjectDoc`. |
| 12 | Add a new agent-to-agent (`a2a.send`) workflow variant | workflows | `guard/` decide function (permissions/agent-to-agent module) | **No** | design-laws.md's LAW-07 annotation is explicit: only the CLI-derived `restart` guard and self-mod's two checks are Go-parity targets; `a2a.send`'s own `decide` function is named as one of the ones that "gates nothing the kernel controls" and stays TS. |
| 13 | Register a new delivery action (`registerDeliveryAction`) | workflows | `delivery.ts` action registry | **No** | host-decomposition.md #5: the action registry is a first-class extension point, explicitly not the piece of delivery that moved to `internal/delivery` (only `ResolveDeliveryTarget`/`ClassifyOutboundMessage`/`NextAttempt` did). |
| 14 | Switch a channel's `engage_mode` between `pattern`/`mention`/`mention-sticky` | trigger word (mode variant of #1) | Data (`messaging_group_agents.engage_mode`) | **No** | Same reasoning as #1 — `EngageMode` is a Go-side enum already handling all three values; switching which one a wiring uses is a config write, not a code change. |
| 15 | Add a new agent provider (OpenCode) | channel (provider variant) | `providers` registry branch + `src/providers/<name>.ts` | **No — but exercises the Go boundary as data** | Provider registration follows the same fetch-and-copy pattern as #10. Its host-config fn may contribute mounts (`VolumeMount`, `origin: 'provider'` — the real PR #3680 mechanism) that flow into `mount.ValidateSpec` unmodified; this is the concrete scenario P6-04 tests explicitly (see that report). |
| 16 | Set a per-wiring thread policy override for a threaded platform | custom route (thread variant) | Data (`wiring.threads` override) | **No** | `routing.ResolveThreadPolicy` (Go, P4-02) already takes the override as a nilable int parameter; setting it is a config write reaching an unmodified function. |
| 17 | Add a skill that mounts a per-agent credential stub via the OneCLI gateway pattern | MCP tool (credential variant of #7) | `src/gateway-providers/<name>.ts` | **No — but exercises the Go boundary as data** | Exactly the real, already-shipped PR #3680 shape: a gateway-contributed mount stamped `origin: 'provider'`, validated by `internal/mount`'s `mountAllowed` (Go, Phase 5) with zero changes to that package — the fix that made this possible was entirely a TS-side field addition. |
| 18 | Customize the free-text agent-naming flow during channel registration | custom route (approval variant) | `src/modules/permissions/index.ts` | **No** | host-decomposition.md #11: this whole module is "the clearest example... of ordinary customization that Design Law says must never require Go." |
| 19 | Add a cross-session-context fan-in rule | workflows | `cross-session-context` module (dynamic-import hook) | **No** | Same hook-seam class as #8/#13 — orchestration stays TS per host-decomposition.md #6's own framing of `host-sweep.ts`'s dynamic imports. |
| 20 | Cap a specific agent group's container memory/CPU below the platform default | multiple agents (resource variant) | Data (`SessionResources` in composed spec) | **No — but exercises the Go boundary as data** | `containerdefaults.ValidateResources`/`EnforceSafeDefaults` (Go, P5-03) accept any positive value and reject only unsafe ones (0 or negative pids-limit, per that package's own doc comment); tightening a cap is ordinary composition data through an unmodified validator, not a new Go rule. |

## Reading the "exercises the Go boundary as data" scenarios

Five scenarios (5, 9, 15, 17, 20) are genuinely different from the other fifteen: they don't just avoid Go entirely, they actively flow through Go-owned validation (`mount.ValidateSpec`, `containerdefaults`, `routing`) and still require **No** Go change. This is the customization/security split the whole project exists to prove is possible: TypeScript composes what varies (which sender gets a bypass, which provider mounts a credential stub, how large a container may be); Go validates a fixed, unchanging structural contract. A catalogue that only contained the other fifteen (pure-TS, never touching a Go-owned function) would prove LAW-02 in the easy case and say nothing about the harder, more valuable claim — that even scenarios adjacent to the security kernel don't need to cross into it.

## What would make a scenario **Yes**

For contrast, since none of the 20 above are: a Go change would be expected only if a customization needed the *set of enforced invariants itself* to change — e.g., "allow a container to mount an arbitrary host path the operator names at runtime" would require touching `internal/mount`'s class/allowlist logic, because that is asking the kernel to validate a genuinely new rule, not to accept new data under an existing one. No such request appears in this catalogue because none of the twelve master-plan-named customization categories (trigger word, agent name, greeting, formatting, custom route, skill, MCP tool, scheduler, multiple agents, channel, prompts, workflows) are, by nature, requests to change what the kernel is willing to enforce.

## Feeds

- **P6-02** (this phase's other deliverable): scenarios 1, 5, 9, 14, 16, 17, 20 above are exactly the shapes `internal/kernel`'s `route.request` and `capability.request` ops were designed against — see `docs/ADR-008-p6-02-ts-go-enforcement-boundary.md`.
- **P6-03**: selects 10 of the 20 above (favoring the "exercises the Go boundary as data" ones, since they're the ones worth demonstrating, not just asserting) and proves each concretely.
- **P6-04**: scenario 15 (new provider) and scenario 17 (credential-stub mount) are tested against real, already-shipped NanoClaw extensions (OneCLI, the provider-container registry) for that report.
- **P6-05**: computes the preservation percentage across all 20.
