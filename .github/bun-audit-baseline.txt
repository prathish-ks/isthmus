# Accepted high+ bun-audit advisories for container/agent-runner/, checked
# by container/agent-runner/scripts/check-bun-audit-baseline.ts (see that
# file for how this is used). Empty as of 2026-09-23: a first real audit
# found 38 findings (9 high, 26 moderate, 3 low), all fixed — see
# docs/agent-runner-audit-findings.md for the full disposition. Nothing
# accepted-but-unfixed exists yet.
#
# To clear an entry: fix it (bun audit fix, or an explicit `overrides`
# entry). To accept a genuinely new one: add its GHSA id below with a
# one-line reason.
#
# module  GHSA id  title
